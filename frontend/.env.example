# Leave empty for local dev: same-origin /api via Vite proxy (no CORS).
# Or set full backend URL e.g. http://127.0.0.1:8000 (Django CORS must allow the SPA origin).
VITE_API_BASE_URL=

# Optional: Auth0 social connection name for Slack (Authentication → Social → Slack). When set,
# “Log in / Sign up with Slack” buttons appear on the home and nav menus.
# VITE_AUTH0_SLACK_CONNECTION=slack

# Sentry (optional). Unset locally to disable. Create a Browser/React project in Sentry and paste the DSN.
# VITE_SENTRY_DSN=https://...@....ingest.sentry.io/...
# VITE_SENTRY_ENVIRONMENT=production
# VITE_SENTRY_TRACES_SAMPLE_RATE=0
