import { Box, Heading, Text } from "@chakra-ui/react";

function App() {
  return (
    <Box p="8">
      <Heading mb="4">Barebones App</Heading>
      <Text>Django + DRF + React + TypeScript + Chakra UI</Text>
    </Box>
  );
}

export default App;
