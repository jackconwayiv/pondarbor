/** Soft parchment canvas for in-game play (not site sky blue). */
export const ESTATES_PLAY_CANVAS_BG = "#E5DDD0";

/** In-game typography (matches playCanvas.css --estates-font-sans). */
export const ESTATES_GAME_FONT_FAMILY =
  '"Spinnaker", Verdana, Geneva, "DejaVu Sans", sans-serif';

/** Server-side pause between automated scoring steps (see backend estates.constants). */
export const SCORING_STEP_DELAY_MS = 2000;
