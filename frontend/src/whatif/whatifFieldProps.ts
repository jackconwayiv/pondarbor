/** Shared field look aligned with quotes inputs (bordered, on white `bg`). */
export const whatifInputProps = {
  bg: "bg",
  borderWidth: "1px",
  borderColor: "border",
  borderRadius: "md",
} as const;
