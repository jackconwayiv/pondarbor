import { PANEL_FIELD_PROPS } from "../theme/typography";

/** Alias of `PANEL_FIELD_PROPS` for What If screens (bordered field + canonical placeholders). */
export const whatifInputProps = PANEL_FIELD_PROPS;
