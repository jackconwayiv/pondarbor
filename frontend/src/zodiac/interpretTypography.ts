/** Shared interpret-tab typography (placement, house, and sign pages). */
export const INTERPRET_BODY_FONT_SIZE = { base: "sm", md: "lg" } as const;

export const INTERPRET_HEADING_SIZE = { base: "md", md: "lg" } as const;
